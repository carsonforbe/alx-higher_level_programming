//attach event listenet to a <div> element with id "red_button"
$('#red_header').click(function() {
	//selects the <header element and updates the color to red
	$('header').css('color', 'FF0000');
});
